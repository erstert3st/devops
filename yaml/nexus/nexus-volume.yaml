apiVersion: v1
kind: PersistentVolume
metadata:
  name: nexus-volume2
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/opt/kubeStorage/nexus"
