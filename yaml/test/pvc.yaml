apiVersion: v1
kind: PersistentVolumeClaim
metadata:
 name: my-pvc
 labels:
   billingType: "hourly"
spec:
 accessModes:
   - ReadWriteOnce
 resources:
   requests:
     storage: 10Gi
 storageClassName: ibmc-block-silver
